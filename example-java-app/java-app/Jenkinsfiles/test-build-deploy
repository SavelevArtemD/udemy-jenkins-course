pipeline {

    agent any
    tools {
        maven 'maven-3.8.2'
        jdk 'JDK'
    }

    stages {
        stage('Test') {
            withMaven {
                sh 'mvn -B -DskipTests clean package'
            }
        }
    }
}